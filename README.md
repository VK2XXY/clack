# clack

An analog clock built with Python and Tkinter.

## Requirements

* Python 3.x (Tkinter is included in the standard library — no extra packages needed)

## Run

```
python clock.py
```

The clock window displays the current local time with hour, minute, and second hands and updates every second.
